import 'package:flutter/material.dart';

class PublicCv extends StatelessWidget {
  const PublicCv({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text("Public Cv")),);
  }
}